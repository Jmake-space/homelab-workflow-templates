# scheduled-healthcheck

Checks a URL every 30 minutes.

## Usage
- Set secret `CHECK_URL` in the repo, or pass a `url` input on manual run.

## Secrets
- `CHECK_URL` (optional): default URL to check.
