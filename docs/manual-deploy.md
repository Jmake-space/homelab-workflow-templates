# manual-deploy

Manual kubectl apply from a path on the runner.

## Usage
- Set `namespace` and `manifest_path` inputs.
- The path must exist on the runner machine.

## Requirements
- `kubectl` installed on the runner.
